<h2 class="page-header"><%= @hierarchy.name %></h2>

<p>
  <strong>Классы этой иерархии:</strong>
</p>

<dl class="dl-horizontal">
<% @hierarchy.classifications.each do |klass| %>
  <dd><%= link_to klass.name, klass %></dd>
<% end %>
</dl>


<%= link_to 'Edit', edit_hierarchy_path(@hierarchy) %> |
<%= link_to 'Back', hierarchies_path %>
